We need to send the webhook events for loggin and loggout, and device state change

# RULES
1. Use the current style of the project
2. Always be care about memmory managment and save
3. Always be care about security

# PLAN
Analize my requirements and create a plan of implementation on ./tasks/[the-plan].md.
Do not change the current code all the suggestions and the ideas of change must exist in ./tasks/[the-plan].md as "```go ...code... ```".
Each code creation/idea must declare where must be,
example: ./src/infrastructure/chatstorage/sqlite_repository.go, line 344; or ./src/infrastructure/chatstorage/sqlite_repository.go change SaveDeviceRecord function
